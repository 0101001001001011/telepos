import 'package:telepos/core/constants/enums/user_role.dart';

abstract class PermissionKeys {
  PermissionKeys._();

  static const navSale = 'nav.sale';
  static const navRefund = 'nav.refund';
  static const navShift = 'nav.shift';
  static const navHistory = 'nav.history';
  static const navCatalog = 'nav.catalog';
  static const navAgent = 'nav.agent';
  static const navSupply = 'nav.supply';
  static const navCashOperation = 'nav.cashOperation';
  static const navSettings = 'nav.settings';
  static const navSync = 'nav.sync';
  static const navReports = 'nav.reports';
  static const navServiceQueue = 'nav.serviceQueue';
  static const navServiceIntake = 'nav.serviceIntake';
  static const navTables = 'nav.tables';
  static const navOrders = 'nav.orders';

  // # Восемь операционных ключей: три из них уже работают, пять — ещё нет
  //
  // Пункт 6 финальной волны закрытия долга безопасности (2026-08-22) оставил
  // здесь запись «**ни одна из этих восьми строк не читается ни одним вызовом
  // `hasPermission` во всём `lib/`**». Задачи 9–11 плана «Продажа с
  // браузерного терминала» сделали эту запись неверной про три ключа, и она
  // переписана по измерению (`grep -rn "opEditPrice\|opSellDiscount\|…" lib/`
  // на ветви `feat/browser-sale-wire-sale`, 2026-09-06), а не по плану.
  //
  // **Подключены — читает `WireGuard.check` (`lib/domain/wire/wire_guard.dart`)
  // до вызова обработчика, по объявлению операции в каталоге продажи
  // (`lib/domain/wire/sale_ops.dart`, `SessionAccess.needs`):**
  //
  // - [opSellDiscount] — `sale.setDiscountPercent`, `sale.setDiscountAmount`;
  // - [opEditPrice] — `sale.updatePrice`, `sale.setWholesale` (опт это выбор
  //   ценовой колонки), и подъём **оптового** отложенного чека — сторож этого
  //   выразить не может, потому что «оптовый ли чек» — строка в базе кассы, а
  //   не кадр;
  // - [opDeferSale] — `sale.defer`, `sale.loadDeferred` и подписка
  //   `sale.deferredList` (список показывает имя отложившего кассира, то есть
  //   читающую половину той же возможности).
  //
  // **Второй читатель обоих — корзина кассы (задача 28, 2026-09-15).**
  // `LocalCartService._requireRight` проверяет [opEditPrice] и [opDeferSale]
  // из обязательного довода `DiscountAuthority` тем же приёмом, что
  // [opSellDiscount] (задача 12): экран кассы зовёт корзину мимо провода, и
  // до задачи 28 эти права на самой кассе не проверял никто. Проверка опта
  // при подъёме переехала туда же из `TillOperations`. Проба —
  // `test/data/sale/cart_rights_desktop_test.dart`. Подписка на пул на кассе
  // права не спрашивает — открыто.
  //
  // Достижимость этих трёх проверена **тем путём, каким пойдёт браузер** —
  // кадром по проводу через настоящего сторожа, а не вызовом обработчика:
  // `test/backend/sale_permissions_test.dart` (задача 11). Объявление права в
  // каталоге закреплено отдельно, `test/domain/wire/sale_ops_access_test.dart`.
  //
  // **Ещё не подключены, но подключаются прямо сейчас** (соседние ветви того
  // же плана; на этой ветви их читателя нет):
  //
  // - [opSellDebt] — задача 14, оплата;
  // - [opRefund], [opRefundWithoutReceipt] — задачи 18–19, возвраты.
  //
  // **Остаются плацебо и после всего плана — названы по имени, чтобы их
  // молчание не приняли за упущение:**
  //
  // - [opCashInOut] — внесение и изъятие денег из ящика. Инкассация за
  //   границей этой работы, ни одной операции продажи это не касается, и
  //   выдумывать ему читателя здесь значило бы приписать праву смысл,
  //   которого у него нет;
  // - [opCancelPayment] — отмена уже проведённой оплаты. Снята из задачи 14
  //   целиком как отдельная работа.
  //
  // Про непокрытые пять всё, что было сказано в 2026-08-22, остаётся в силе.
  // `hasPermission` по-прежнему зовут пять мест (`app_router.dart`,
  // `setup_router.dart`, `hasPermissionProvider` в
  // `app_state_controller.dart` — которую по-прежнему никто не зовёт,
  // `terminal_home_screen.dart` дважды), и ни одно не спрашивает `op.*`:
  // подключение сделано **на проводе**, а не на экранах. То есть у кассы
  // остаётся второй фронт — тумблер опта или скидка, нажатые на самом
  // десктопе, мимо провода, права не проверяют ничем.
  // `PointModePermissions.effective` их только *вычитает* у роли по режиму
  // терминала.
  //
  // Тот же критерий, по которому фаза 5 сняла три ключа-плацебо (объявлены,
  // показаны, не читаются ни одним экраном) — но эти восемь **не сняты**:
  // решение — не удалить, а подключить к настоящим проверкам, потому что
  // это операционные права, которые кассе нужны по существу (в отличие от
  // снятых трёх). Граница названа в спеке
  // (`docs/internal/superpowers/specs/2026-08-21-security-debt-closure-design.md`,
  // «Границы»).
  static const opEditPrice = 'op.editPrice';
  static const opSellDiscount = 'op.sellDiscount';
  static const opSellDebt = 'op.sellDebt';
  static const opCashInOut = 'op.cashInOut';
  static const opCancelPayment = 'op.cancelPayment';
  static const opDeferSale = 'op.deferSale';
  static const opRefund = 'op.refund';
  static const opRefundWithoutReceipt = 'op.refundWithoutReceipt';

  /// Выпуск подарочного сертификата — задача 21.
  ///
  /// # Почему выпуску нужно **своё** право, а `nav.sale` мало
  ///
  /// Выпуск создаёт обязательство **из ничего**: строка сертификата с
  /// номиналом и остатком появляется до того, как за неё заплатили, и
  /// платит за неё отдельная строка оплаты того же чека. Кассир, которому
  /// хватило бы `nav.sale`, мог бы выписать себе бумажку на сто тысяч и
  /// отоварить её в соседнюю смену — ни одна проверка оплаты этого не
  /// заметила бы, потому что гашение такой бумажки **законно**.
  ///
  /// # Что значит его отсутствие
  ///
  /// Строки прав нет — значит не разрешено (`getAllowedKeys` собирает
  /// только `isAllowed = true`). Владелец получает `allPermissions` в
  /// обход таблицы (`LocalAuthRepository._issue`), то есть **способ
  /// пройти гейт есть с первой минуты**, а остальным его выдаёт владелец
  /// на экране пользователей — тот перебирает [allPermissions], и новый
  /// ключ появляется в нём сам.
  ///
  /// Умолчаний роли ему **не заведено** (`groups`/`_roleDefaults` его не
  /// называют): решение «этому кассиру можно печатать обязательства
  /// магазина» принимает владелец, а не таблица по умолчанию.
  static const opIssueCertificate = 'op.issueCertificate';

  /// Погашение рассрочки — задача 24.
  ///
  /// # Почему у продажи в рассрочку своего права НЕТ, а у погашения —
  /// есть
  ///
  /// Продажа в рассрочку это **продажа в кредит**, и право у неё то же —
  /// [opSellDebt]. Второе право рядом отвечало бы на тот же вопрос
  /// («кому можно отдать товар за запись в базе») другим словом, и первое
  /// же расхождение дало бы кассира, которому долг запрещён, а рассрочка
  /// разрешена, — то есть запрет, снимаемый выбором кнопки.
  ///
  /// Погашение — вопрос **другой**: «кому можно принять деньги по чужому
  /// договору и уменьшить долг». Это не продажа: товара здесь нет, а есть
  /// движение по счёту задолженности, которое никакой чек не сторнирует.
  /// Ошибка здесь не ловится инвентаризацией и не видна в отчёте смены
  /// (строка `agent_settlement` в выручку не идёт), поэтому право своё.
  ///
  /// Ключ выдаётся кассиру по умолчанию (`roleDefaults`) — деньги по
  /// договору приносят на кассу, и принимать их некому, кроме кассира.
  /// Существующим пользователям его раздаёт миграция v45
  /// (`_grantNewPermissionKeys`): без неё ключ, добавленный в
  /// [allPermissions], молча стал бы отказом для каждого не-владельца.
  static const opCreditRepay = 'op.creditRepay';

  static const settingsUsers = 'settings.users';
  static const settingsAccounts = 'settings.accounts';
  static const settingsPrinter = 'settings.printer';
  static const settingsFiscal = 'settings.fiscal';
  static const settingsHardware = 'settings.hardware';
  static const settingsRestaurant = 'settings.restaurant';
  static const settingsTransport = 'settings.transport';
  static const settingsTelegram = 'settings.telegram';

  // Правка «второй порядок» закрытия долга безопасности (2026-08-22),
  // пункт 1: три маршрута настроек — `/terminal-service-settings`,
  // `/log-journal`, `/appliance-settings` — не были закрыты ничем: ни
  // ключом права, ни `ownerOnlyScaffold` (в отличие от `/system-management`
  // и `/system-terminal`, которых виджет проверяет напрямую). Любой
  // вошедший с любой ролью, включая `UserRole.user` (самую узкую), открывал
  // их прямым переходом по адресу. Три новых ключа, а не переиспользование
  // существующих: ни один из трёх экранов не совпадает по смыслу ни с одним
  // уже заведённым ключом настроек (`settingsHardware` — профили
  // периферии кассы: сканер/весы/денежный ящик/POS-терминал, не сеть и не
  // журнал; `settingsUsers` — кто и как входит, не то же самое, что порт
  // терминалов или системный журнал).
  static const settingsTerminalService = 'settings.terminalService';
  static const settingsLogJournal = 'settings.logJournal';
  static const settingsAppliance = 'settings.appliance';

  /// Полный словарь ключей прав — 34 на 2026-08-22 (правка «второй порядок»
  /// добавила три: [settingsTerminalService], [settingsLogJournal],
  /// [settingsAppliance] — см. комментарий над ними).
  ///
  /// # Граница Б-2 закрытия долга безопасности (2026-08-22): allow-list
  /// замкнут навсегда, если добавить ключ сюда без миграции
  ///
  /// После задачи 16 (`UserPermissionDao.getAllowedKeys`: пустая строка =
  /// запрещено) и правки Б-1 (миграция v32→v33 дописывает разрешающую
  /// строку на каждый ключ существующему не-владельцу) таблица
  /// `user_permissions` — allow-list: ключ действует, только если для него
  /// есть строка `isAllowed = true`. [roleDefaults] читается ОДИН раз —
  /// при заведении нового пользователя (мастер, форма создания). Больше
  /// никто и никогда не дописывает недостающие строки существующим
  /// пользователям — миграция v32→v33 сделала это один раз, для одного
  /// перехода, и не повторяется на каждый новый ключ.
  ///
  /// Значит: **любой ключ, добавленный в этот набор будущей версией, будет
  /// запрещён всем существующим не-владельцам** — у них просто не будет
  /// строки на него, а отсутствие строки теперь и означает запрет. Новый
  /// ключ достаётся только тем, кого заведут ПОСЛЕ его появления в словаре;
  /// все, кто уже существовал, — нет, до тех пор, пока кто-то не даст им
  /// права вручную через экран.
  ///
  /// **Правило на будущее:** добавление ключа в этот набор обязано
  /// сопровождаться шагом миграции (по образцу блока `if (from < 33)` в
  /// `app_database.dart`), который выдаёт новый ключ существующим
  /// пользователям по умолчанию их роли (`roleDefaults[role]`) — иначе
  /// новая возможность тихо не работает ни для кого, кроме тех, кого заведут
  /// заново. Граница названа явно в спеке
  /// (`docs/internal/superpowers/specs/2026-08-21-security-debt-closure-design.md`,
  /// «Границы»).
  static const Set<String> allPermissions = {
    navSale,
    navRefund,
    navShift,
    navHistory,
    navCatalog,
    navAgent,
    navSupply,
    navCashOperation,
    navSettings,
    navSync,
    navReports,
    navServiceQueue,
    navServiceIntake,
    navTables,
    navOrders,
    opEditPrice,
    opSellDiscount,
    opSellDebt,
    opCashInOut,
    opCancelPayment,
    opDeferSale,
    opRefund,
    opRefundWithoutReceipt,
    opIssueCertificate,
    opCreditRepay,
    settingsUsers,
    settingsAccounts,
    settingsPrinter,
    settingsFiscal,
    settingsHardware,
    settingsRestaurant,
    settingsTransport,
    settingsTelegram,
    settingsTerminalService,
    settingsLogJournal,
    settingsAppliance,
  };

  static const Map<String, List<String>> groups = {
    'navigation': [
      navSale,
      navRefund,
      navShift,
      navHistory,
      navCatalog,
      navAgent,
      navSupply,
      navCashOperation,
      navSettings,
      navSync,
      navReports,
      navServiceQueue,
      navServiceIntake,
      navTables,
      navOrders,
    ],
    'operations': [
      opEditPrice,
      opSellDiscount,
      opSellDebt,
      opCashInOut,
      opCancelPayment,
      opDeferSale,
      opRefund,
      opRefundWithoutReceipt,
      opIssueCertificate,
      opCreditRepay,
    ],
    'settings': [
      settingsUsers,
      settingsAccounts,
      settingsPrinter,
      settingsFiscal,
      settingsHardware,
      settingsRestaurant,
      settingsTransport,
      settingsTelegram,
      settingsTerminalService,
      settingsLogJournal,
      settingsAppliance,
    ],
  };

  /// Что получает **новый** пользователь, заведённый мастером, если никто
  /// ничего не выбирал руками.
  ///
  /// Единственное место, где умолчания по роли вообще существуют — их не
  /// было в проекте нигде до задачи 12. `owner` здесь — для полноты и
  /// симметрии с правилом ниже, а не потому что его кто-то читает:
  /// `LocalAuthRepository._issue` выдаёт владельцу [allPermissions] в обход
  /// этой таблицы и таблицы прав целиком.
  ///
  /// # Кто на самом деле читает эту карту (поправлено правкой Б-4, 2026-08-22)
  ///
  /// Только мастер первого запуска — `SetupRepositoryLocal
  /// ._writeRoleDefaultPermissions` (задача 13), заводящий не-владельцев без
  /// экрана прав вовсе. Форма из настроек (`user_management_screen.dart`,
  /// `_UserEditDialogState._loadPermissions`, ветка `isCreating`) эту карту
  /// **не читает**: она включает все переключатели по умолчанию для любой
  /// роли — человек видит каждый ключ на экране и должен снять лишнее сам,
  /// прежде чем нажать «Сохранить». Старая формулировка этого абзаца
  /// («применяется только к заведению новых не-владельцев, задача 13») эту
  /// разницу стирала — читалась как «применяется к любому заведению нового
  /// пользователя», что для формы неверно.
  ///
  /// Миграция существующих пользователей (задача 15, `if (from < 33)` в
  /// `app_database.dart`) эту карту тоже не читает — она пишет каждому то,
  /// что действует сегодня (недостающие ключи — как разрешённые, правка
  /// Б-1), а не умолчание по роли.
  ///
  /// **Граница Б-2** (см. докстринг [allPermissions]): это единственное
  /// место, где умолчания по роли применяются, — и только при заведении. Ни
  /// один механизм не дописывает недостающие ключи существующим
  /// пользователям на постоянной основе; добавление ключа в [allPermissions]
  /// требует отдельного шага миграции, иначе он тихо не достаётся никому,
  /// кроме заведённых заново.
  static final Map<UserRole, Set<String>> roleDefaults = {
    // Обходит таблицу прав в LocalAuthRepository._issue — значение ниже не
    // проверяется рантаймом, но правило то же самое: владелец не сужается.
    UserRole.owner: allPermissions,

    // Всё, кроме управления пользователями. `settings.users` — это право
    // назначать роли и PIN другим, то есть право раздавать права; отдав его
    // администратору, отдаёшь ему и путь самоповышения (завести себе или
    // другому администратору права владельца через ту же форму). Это
    // единственное, что «явно принадлежит владельцу» — линия проведена по
    // одному ключу, а не по вкусу.
    //
    // Правка Б-4 закрытия долга безопасности (2026-08-22): до этой правки
    // здесь стоял рукописный литерал — те же 30 ключей, но набранные
    // вручную, — а докстринг класса обещал «вычисляется от [allPermissions],
    // новый ключ появится сам». Расхождение молчало бы: новый ключ,
    // добавленный в словарь, тихо НЕ появлялся бы у администратора, пока
    // кто-то не вспомнил бы дописать его в этот литерал руками. Приведено к
    // докстрингу, не наоборот, — вычисляемое множество, а не переписанное:
    // администратор получает всё, кроме единственного явно исключённого
    // ключа, и это верно как автоматическое умолчание для нового ключа —
    // «всё, что не раздача прав» ближе к намерению роли, чем «то, что было
    // явно перечислено на момент, когда писали этот код».
    UserRole.administrator: {...allPermissions}..remove(settingsUsers),

    // Самый узкий набор из всех четырёх: никакой строки кода в проекте не
    // отличает `UserRole.user` предметно (только цвет бейджа в
    // user_management_screen.dart и место в `role.index <=
    // UserRole.administrator.index`, где эта роль — НЕ супервизор наравне с
    // кассиром). Раз назначение роли предметно не находится, умолчание не
    // придумывает его: только то, что не открывает ни денег, ни настроек —
    // посмотреть историю продаж и каталог, не более.
    UserRole.user: {navHistory, navCatalog},

    // Торговля и смена — как велит бриф — и ничего из настроек. Список
    // навигации — рабочее место кассира во всех четырёх режимах
    // (nav_destinations.dart): помимо retail-троицы sale/refund/shift это
    // также tables/orders (restaurant) и serviceQueue/serviceIntake
    // (service) — те же самые «продаю сейчас», только под другим именем в
    // другом режиме. cashOperation — внесение/изъятие по смене, тоже часть
    // «смены», не настроек. history — свой же чек посмотреть.
    //
    // Исключены из навигации: agent (это не продажа, а ведение картотеки
    // контрагентов — CRUD-экран, а не выбор покупателя на кассе),
    // supply (приёмка товара — складская операция, не торговая), reports
    // (аналитика по всей кассе — управленческая функция), sync — и, по
    // прямому требованию брифа, settings и всё settings.* целиком: ни один
    // settings-ключ кассиру не достаётся, хотя до этой задачи пустая
    // таблица прав отдавала ему их все.
    //
    // Операции: скидка, продажа в долг, внесение/изъятие, отложенная
    // продажа и обычный возврат (по чеку) — обыденные действия у кассы.
    // Не достаются: editPrice (произвольная правка цены — путь слить
    // маржу), cancelPayment (отмена уже проведённой оплаты — деньги
    // фактически взяты, отмена без чужого подтверждения — путь кражи) и
    // refundWithoutReceipt (возврат без чека — самый дешёвый способ обнала
    // без следа). Эти три — ровно то, ради чего в системе вообще есть
    // роль-супервизор, авторизующая операцию поверх кассира
    // (RoleIdentificationServiceImpl.identify, role.index <=
    // UserRole.administrator.index): без них кассир не может провернуть их
    // в одиночку.
    UserRole.cashier: {
      navSale,
      navRefund,
      navShift,
      navHistory,
      navCatalog,
      navCashOperation,
      navTables,
      navOrders,
      navServiceQueue,
      navServiceIntake,
      opSellDiscount,
      opSellDebt,
      opCashInOut,
      opDeferSale,
      opRefund,
      opCreditRepay,
    },
  };

  /// Карта маршрут → ключ права, читаемая [routeToPermissionKey].
  ///
  /// Правка «маршрут → ключ» закрытия долга безопасности (2026-08-22):
  /// восемь `settings.*`-маршрутов ниже добавлены сюда, а не второй
  /// параллельной картой в `app_router.dart` — задача 17 сознательно не
  /// стала туда лезть именно потому, что вторая карта рядом с этой была бы
  /// тем самым дублированием, из-за которого долг и возник.
  /// `/user-management` —
  /// самый острый: это ровно путь самоповышения, который докстринг
  /// [roleDefaults] называет причиной, по которой `settings.users` не
  /// достаётся администратору.
  static const Map<String, String> _routePermissions = {
    '/sale': navSale,
    '/refund': navRefund,
    '/shift': navShift,
    '/history': navHistory,
    '/catalog': navCatalog,
    '/agent': navAgent,
    '/supply': navSupply,
    '/cash-operation': navCashOperation,
    '/settings': navSettings,
    '/sync': navSync,
    '/reports': navReports,
    '/service-queue': navServiceQueue,
    '/service-intake': navServiceIntake,
    '/tables': navTables,
    '/orders': navOrders,
    '/printer-settings': settingsPrinter,
    '/fiscal-settings': settingsFiscal,

    // Налоги — тем же ключом, что и фискальная настройка, и намеренно не
    // своим. Аудитория одна: ставки и фискализация — две стороны одного
    // разговора с налоговой, и держатель одного ключа неизбежно держит
    // второй. Новый ключ пришлось бы раздавать миграцией, и до неё он молча
    // стал бы отказом для каждого, кто настройку налогов уже вёл.
    '/tax-settings': settingsFiscal,
    // Часы запрета продажи — та же ответственность, что у налога:
    // правило, за которое отвечают перед проверяющим.
    '/selling-hours': settingsFiscal,
    '/hardware-settings': settingsHardware,

    // Встроенные эмуляторы — тем же ключом, что и оборудование, и намеренно
    // не своим. Эмулятор ничего не подменяет: он открывает сокет, на который
    // касса попадёт только через привязку прибора, а привязку правит держатель
    // ровно этого ключа. Новый ключ пришлось бы раздавать миграцией, и до неё
    // он молча стал бы отказом для каждого не-владельца — цена, которую здесь
    // не за что платить.
    '/emulator-settings': settingsHardware,

    // Диагностика — тем же ключом и по тому же доводу. Экран показывает
    // байты, ушедшие в принтер, и тело запроса к оператору фискализации: это
    // внутренности настройки оборудования, а не операция кассира. Своего
    // ключа нет намеренно — он потребовал бы миграции.
    '/diagnostics': settingsHardware,

    // То же с планшета — пункт «Достижимость с браузерного терминала» плана
    // 2026-09-19. Ключ **тот же**, и это существенно: маршрут браузерной
    // таблицы без записи здесь открылся бы любому вошедшему (её `redirect`
    // спрашивает право ровно этой картой), и две операции провода
    // `diagnostics.*`, охраняемые `settings.hardware`, остались бы
    // единственной границей — то есть кассир без права увидел бы экран и
    // получил бы на нём два отказа вместо честного «сюда нельзя».
    //
    // В счёт `route_permission_coverage_test.dart` этот маршрут не входит и
    // не должен: тот тест обходит `createRouter()` — десктопную таблицу, — а
    // здесь маршрут объявлен только браузерной. Тем же свойством и по тому
    // же доводу там не считается `/terminal-shift`.
    '/terminal-diagnostics': settingsHardware,
    '/restaurant-settings': settingsRestaurant,
    '/transport-settings': settingsTransport,
    '/telegram-settings': settingsTelegram,

    // Блокер 2 финальной волны закрытия долга безопасности (2026-08-22):
    // `/telegram-setup` (`TelegramAuthScreen` — телефон, код, авторизация
    // учётной записи Telegram, один из двух оставшихся транспортов) не был
    // в этой карте вовсе и проходил `redirect` любому вошедшему без
    // проверки права. `route_permission_coverage_test.dart` объявлял его
    // «нет ключа права ни для одной роли» — неправда: тот же периметр, что
    // и у соседнего `/telegram-settings` прямо над этой строкой, тот же
    // ключ.
    '/telegram-setup': settingsTelegram,
    '/accounts-settings': settingsAccounts,
    // Пункт 8 C (2026-09-15): настройка оплаты по QR. Ключ существующий —
    // провайдер решает, куда уходят деньги покупателя, тот же периметр, что
    // и счета кассы; новый ключ без шага миграции не достался бы никому
    // (докстринг [allPermissions], «Правило на будущее»).
    '/qr-provider-settings': settingsAccounts,

    // Выпуск подарочного сертификата — дыра 1 ревизии 2026-09-19. До неё
    // `op.issueCertificate` не звал **ни один** файл `lib/presentation/`:
    // право было объявлено, роздано на экране пользователей, проверено
    // сторожем провода — и не охраняло ничего, что кассир мог бы нажать,
    // потому что нажимать было нечего.
    //
    // Ключ **существующий и свой** — редкий случай, когда новый заводить не
    // надо и брать чужой тоже: `opIssueCertificate` написан ровно под эту
    // операцию (его докстринг разбирает, почему `nav.sale` мало), просто до
    // сего дня жил только на проводе. Умолчаний роли у него нет намеренно —
    // владелец решает, кому можно печатать обязательства магазина, — и
    // кассир без этого ключа сюда не войдёт вовсе.
    //
    // Право **двигать обязательство** маршрутом не выдаётся и не отнимается:
    // операцию провода `pay.certificateIssue` касса охраняет тем же ключом
    // сверх `nav.sale`, кассовый экран спрашивает его перед вызовом выпуска,
    // а сам выпуск — из обязательного довода полномочий (ревизия второго
    // фронта 2026-09-19, `CertificateIssuer.issue`). Маршрут — первая из
    // четырёх дверей, а не единственная (I162); последняя — единственная,
    // которую нельзя обойти, открыв экран мимо `redirect`.
    '/certificate-issue': opIssueCertificate,

    // Выдача аванса деньгами — дыра 2 ревизии 2026-09-19. Ключ тот же, что
    // у **приёма** на проводе (`pay.prepaymentIntake`), и это не экономия
    // на новом ключе, хотя новый и потребовал бы миграции (докстринг
    // [allPermissions], «Правило на будущее»).
    //
    // Довод по существу: приём и выдача — одна работа, повёрнутая двумя
    // сторонами. Тот же расчётный счёт покупателя, то же движение по нему,
    // тот же документ у оператора (настройка одна —
    // `fiscalizePrepaymentReceipt`, докстринг
    // `CustomerPaymentUseCase.refundPrepayment`). Разделить их двумя
    // ключами значило бы утверждать, что бывает кассир, которому можно
    // взять чужие деньги на счёт, но нельзя их отдать, — а это не
    // осторожность, а ловушка: деньги приняты, вернуть их некому.
    //
    // `nav.cashOperation` (ключ браузерного `/prepayment`) здесь **мало**:
    // тот ключ говорит «можно открыть ящик», а не «можно уменьшить чужое
    // сальдо». [opCreditRepay] кассиру тоже полагается по умолчанию и
    // роздан миграцией v45 — значит выдача работает с первой минуты, а не
    // после того, как владелец вспомнит.
    '/prepayment-refund': opCreditRepay,

    // Приём аванса покупателя с браузерного терминала — требование
    // заказчика 2026-09-18. Ключ существующий, а не новый: новый без шага
    // миграции тихо не достаётся ни одному существующему пользователю
    // (докстринг [allPermissions], «Правило на будущее»), а миграции у этой
    // работы нет.
    //
    // Почему именно [navCashOperation]: экран принимает у покупателя живые
    // деньги и кладёт их в кассу — это кассовая операция, а не продажа
    // (чека здесь нет вовсе). Тот же периметр, что у внесения и изъятия по
    // смене, и кассиру он полагается по умолчанию ([roleDefaults]).
    //
    // Право **двигать счёт расчётов** этим не выдаётся: операцию провода
    // `pay.prepaymentIntake` касса охраняет `op.creditRepay` сверх
    // `nav.sale`, и открытый маршрут её не отпирает.
    '/prepayment': navCashOperation,
    // Шаблон чека с браузерного терминала — решение заказчика 2026-09-18.
    // До этого дня обоих маршрутов в этой карте не было вовсе, и
    // `route_permission_coverage_test` держал их в списке «нет ключа права
    // ни для одной роли». Браузерной таблице ключ нужен: `redirect`
    // проверяет право маршрута, и без записи экран открылся бы любому
    // вошедшему.
    //
    // Ключ существующий, а не новый: новый без шага миграции тихо не
    // достаётся ни одному существующему пользователю (докстринг
    // [allPermissions], «Правило на будущее»), а миграции у этой работы нет.
    //
    // Почему именно [settingsPrinter]: шаблон осмыслен только вместе с
    // шириной ленты, которую задаёт `/printer-settings` под этим же ключом,
    // — «узкий чек при выбранном широком» и был тем дефектом, из-за
    // которого ширину из шаблона убрали. Кто настраивает принтер, тот и
    // правит его чек. Кассиру ни один `settings.*` не достаётся
    // ([roleDefaults]) — и это верно: текст чека магазина не рядовое
    // действие у кассы.
    '/receipt-templates': settingsPrinter,
    // Смена браузерного терминала — решение заказчика 2026-09-18. Ключ тот
    // же, что у `/shift` на кассе ([navShift]), и тот же периметр: это тот
    // самый экран, только на другой поверхности. Кассиру он достаётся по
    // умолчанию ([roleDefaults]) — закрыть свою смену рядовое действие у
    // кассы, ради которого кассир не должен искать администратора.
    '/terminal-shift': navShift,
    '/user-management': settingsUsers,

    // Задача 18, закрытие И31: walk-up и срок сеанса — то же решение «кто и
    // как входит в кассу», что и управление пользователями, поэтому тот же
    // ключ, а не собственный. Отдельный ключ потребовал бы отдельного шага
    // миграции (см. докстринг [allPermissions], «Правило на будущее») ради
    // экрана, который семантически — часть той же самой границы, что и
    // `/user-management`: администратор не получает его по умолчанию по той
    // же причине — самоповышение через смену политики входа не менее острое,
    // чем через смену роли.
    '/auth-settings': settingsUsers,

    // Задача 19 закрытия долга безопасности: список живых сеансов и их
    // отзыв — тот же периметр «кто и как входит в кассу», что и у
    // `/auth-settings` и `/user-management` прямо над ней. Отзыв чужого
    // сеанса не то же самое, что настройка оборудования
    // (`settings.hardware`): решение здесь — кого вообще пускать в кассу
    // дальше, а не что к ней подключено.
    '/sessions': settingsUsers,

    // Задача 2 работы «знакомство терминала с кассой» (2026-08-23): экран
    // кода привязки. Тот же периметр «кто и как входит в кассу», что и у
    // `/sessions` прямо над ней — код привязки решает, кто вообще может
    // завести себе новый терминал, а не что к кассе подключено
    // (`settings.hardware`/`settings.terminalService`). Отдельного ключа не
    // заводим: новый ключ потребовал бы отдельного шага миграции
    // ([allPermissions], «Правило на будущее»), а выигрыша не даёт.
    '/terminal-pairing': settingsUsers,

    // Задача 19 плана «полнота продажи» (2026-09-07), пункт меню на акции.
    // Экран акций получает вход из настроек — и вместе с ним ключ права, а
    // не «пускать любого вошедшего», каким он значился до сих пор
    // (`route_permission_coverage_test.dart`, `_intentionallyOpenRoutes`:
    // «нет ключа права ни для одной роли»).
    //
    // Ключ взят существующий, а не заведён новый: новый ключ пришлось бы
    // сопровождать шагом миграции (докстринг [allPermissions], «Правило на
    // будущее»), иначе он тихо не достался бы ни одному существующему
    // пользователю — тот же довод, каким `/auth-settings`, `/sessions` и
    // `/terminal-pairing` выше переиспользуют [settingsUsers].
    //
    // Почему именно [opEditPrice], а не settings.*: акция — это постоянно
    // действующее изменение того, сколько платит покупатель («купи 2 →
    // третий бесплатно»). Периметр тот же, что у разовой правки цены в
    // чеке, и [roleDefaults] уже провёл по нему линию явным доводом —
    // кассиру [opEditPrice] не достаётся, потому что это «путь слить
    // маржу». Право заводить акции — тот же путь, только не на один чек, а
    // на все. Владелец и администратор ключ имеют, кассир — нет.
    '/promotions': opEditPrice,

    // Задача 12 плана «полнота продажи» (2026-09-07): экран пределов
    // скидки. Ключ — [opEditPrice], тот же, что у соседней строки
    // `/promotions`, и по тому же доводу: и акция, и предел скидки решают,
    // сколько маржи разрешено отдать. [roleDefaults] уже провёл по этому
    // периметру линию явным доводом — кассиру [opEditPrice] не достаётся,
    // потому что это «путь слить маржу»; человек, назначающий предел
    // **самому себе**, обессмыслил бы предел в первый же день.
    //
    // Новый ключ не заводится по тому же правилу, каким его не завели
    // задачи 11 и 19: ключ без шага миграции тихо не достаётся ни одному
    // существующему пользователю (докстринг [allPermissions], «Правило на
    // будущее»), а миграция этой задачи занята таблицей.
    '/discount-limits': opEditPrice,

    // Задача 11 плана «полнота продажи» (2026-09-07): экран
    // нефискализованных чеков.
    //
    // Ключ взят существующий — [settingsFiscal], — а НЕ заведён новый
    // `op.fiscalRetry`, как называл план. Довод тот же, каким задача 19
    // строкой выше переиспользовала [opEditPrice], и он записан в
    // докстринге [allPermissions], «Правило на будущее»: новый ключ тихо
    // не достаётся ни одному существующему пользователю, пока его не
    // раздаст отдельный шаг миграции схемы. Миграция в этой задаче не
    // делается намеренно (колонка `Sales.fiscalState` — задача 14), а
    // класть шаг миграции ради одного ключа значило бы либо занять номер
    // схемы у задачи 12, либо оставить право не работающим ни для кого.
    //
    // Почему именно `settings.fiscal`, а не операционный ключ: экран
    // решает судьбу **документа**, которого у продажи не будет, — повтор
    // к оператору и списание с именем. Тот же периметр, что и у экрана
    // фискальных настроек, из которого в него и входят: кто настраивает
    // оператора, тот и разбирает его отказы. Кассиру ни один `settings.*`
    // не достаётся ([roleDefaults]) — и это верно: списание чека без
    // документа не рядовое действие у кассы.
    '/unfiscalized-receipts': settingsFiscal,

    // Договоры рассрочки — задача 24. Ключ **свой** (`op.creditRepay`), а
    // не `op.sellDebt`: продажа в кредит и приём денег по чужому договору
    // — разные вопросы. Ошибка при погашении не ловится ни
    // инвентаризацией, ни отчётом смены (строка `agent_settlement` в
    // выручку не идёт), поэтому право отдельное. Новый ключ раздаёт
    // миграция v45 — без неё он молча стал бы отказом для каждого
    // существующего не-владельца.
    '/credit-contracts': opCreditRepay,

    // Правка «второй порядок» закрытия долга безопасности (2026-08-22),
    // пункт 1: три маршрута без ключа и без ownerOnlyScaffold — см.
    // комментарий над [settingsTerminalService].
    '/terminal-service-settings': settingsTerminalService,
    '/log-journal': settingsLogJournal,
    '/appliance-settings': settingsAppliance,
  };

  /// Ключ права на маршрут, сопоставленный **по префиксу**, а не по
  /// равенству строк.
  ///
  /// До этой правки сравнение было литеральным (`switch (route) { '/tables'
  /// => ... }`), поэтому `/tables/:tableId`, `/orders/:orderId` и
  /// `/service-queue/:orderId` не совпадали ни с одним case и проходили
  /// сторож свободно — при том что их родительские списки (`/tables`,
  /// `/orders`, `/service-queue`) защищены тем же ключом. Совпадение по
  /// границе `/` (`route == key || route.startsWith('$key/')`), а не голый
  /// `startsWith`: голый `startsWith` дал бы ложное совпадение, будь в
  /// таблице одновременно, например, `/orders` и `/orders-archive` — среди
  /// нынешних ключей такой пары нет (проверено переборкой всех пар в тесте
  /// `permission_keys_test.dart`), но граница ловит это по конструкции, а не
  /// по факту, что сегодня повезло.
  static String? routeToPermissionKey(String route) {
    for (final entry in _routePermissions.entries) {
      if (route == entry.key || route.startsWith('${entry.key}/')) {
        return entry.value;
      }
    }
    return null;
  }

  // `label` и `groupLabel` жили здесь и возвращали тридцать шесть русских
  // слов — «Продажа», «Кассовые операции», «Браузерные терминалы», — и экран
  // управления пользователями рисовал их на любой кассе. Экранный сторож
  // молчал: `lib/core` он не читает.
  //
  // Слово переехало в `presentation/common/utils/permission_label.dart`.
  // Ключ права остался строкой: он хранится в правах пользователя и ездит
  // по проводу, менять его нельзя.
}
