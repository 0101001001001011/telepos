/// Денежный ящик, который открывается, — фон для проб, которым ящик не
/// предмет.
///
/// Задача 41 сделала `drawer:` у `LocalPaymentService` **обязательным**
/// доводом: пустой порт давал тот же `null`, что «ящик открылся», и касса,
/// собранная без ящика, была неотличима от кассы, у которой всё в порядке.
/// Пробы, которые про деньги, фискализацию или QR, про ящик не спрашивают, —
/// им нужен ящик, который не мешает, а не отсутствие ящика. Пробы, для
/// которых ящик — предмет (`completion_side_effects_test.dart`), строят свой.
Future<bool> drawerOpens() async => true;
