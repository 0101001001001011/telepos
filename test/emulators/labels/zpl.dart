/// Разборщик ZPL/EPL переехал в `lib/` вместе с эмулятором ESC/POS: тот же
/// процесс разбирает оба потока, и делить их по разным деревьям значило бы
/// держать половину прибора в продукте, а половину в пробах.
library;

export 'package:telepos/emulators/labels/zpl.dart';
