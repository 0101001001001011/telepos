/// Разборщик потока ESC/POS переехал в `lib/` вместе с самим эмулятором
/// (решение заказчика 2026-09-19: эмулятор встроен в приложение).
///
/// Пересылка оставлена нарочно: прежние читатели — живые прогоны, стенд
/// чековой печати, сторожа — не меняются ни строкой. Точка подстановки как
/// была сетью, так и осталась.
library;

export 'package:telepos/emulators/escpos/render.dart';
